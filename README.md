# Cycle Terminal Color

### Execution:

`$ bash changeColor.sh`

To alter colors change colors in array within `changeColor.sh`

You can change the speed at which the colors change by changing the amount of sleep
